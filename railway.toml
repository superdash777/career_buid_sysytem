# Railway config-as-code: явно используем корневой Dockerfile (Node → frontend/dist, Python → api).
# См. https://docs.railway.com/guides/config-as-code
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
