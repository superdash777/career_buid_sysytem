# Зависимости только для продакшен-API (`python api.py` / Docker).
# Без Gradio, pandas, matplotlib и rouge-score — они нужны `main.py` / eval / скриптам
# и сильно раздувают образ и диск билдера (Railway BuildKit «no space left on device»).
# Локальная разработка: по-прежнему `pip install -r requirements.txt`.

--extra-index-url https://download.pytorch.org/whl/cpu
torch
openai>=1.0,<2
pypdf>=3.0
python-dotenv>=1.0
pymorphy3>=2.0
sentence-transformers>=2.2.0,<3.0
transformers>=4.40
scikit-learn>=1.0
fastapi>=0.100
uvicorn>=0.22
python-multipart>=0.0.9
rapidfuzz
tiktoken>=0.5.0
bcrypt
PyJWT
email-validator
